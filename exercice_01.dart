main() {
 var largeur = 20; // ici, on définie la variable largeur comme 20
 var hauteur = 5 * 9.3; // ici, on défini la variable hauteur comme 5 fois (*) 9,3
 var r = largeur * hauteur; // ici on dit que la variable r est définie comme la multiplication de la variable hauteur fois la variable largeur
 print(r); // ici, on demande d'afficher la variable r, définie à la ligne précédente
}